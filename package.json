{
  "name": "liberteis",
  "version": "0.14.4",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development npx nodemon --ignore 'logs/*' --ignore 'data/*' index.js",
    "prod": "cross-env NODE_ENV=production node index.js",
    "patch": "git push && npm version patch && git push && git push --tags",
    "minor": "git push && npm version minor && git push && git push --tags",
    "major": "git push && npm version major && git push && git push --tags"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "enmap": "^5.9.10",
    "express": "^5.0.1",
    "express-session": "^1.18.1",
    "generate-password": "^1.7.1",
    "i18n": "^0.15.1",
    "multer": "1.4.5-lts.1",
    "nodemailer": "^6.9.15",
    "session-file-store": "^1.5.0",
    "sharp": "^0.33.5"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
