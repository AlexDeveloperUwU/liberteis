<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Eventos Próximos - LiberTeis</title>
  <link rel="icon" href="/assets/img/favicon/favicon.png" type="image/png">
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/assets/js/plugins/qrcodejs/qrcode.js"></script>
  <style>
    @font-face {
      font-family: "PP Agrandir";
      src: local("PP Agrandir Regular"), local("PPAgrandir-Regular"),
        url("/assets/fonts/PPAgrandir-Regular.woff2") format("woff2");
      font-weight: normal;
      font-style: normal;
      font-display: swap;
    }
    

    body, html {
      user-select: none;
    }

    * {
      font-family: "PP Agrandir", sans-serif;
      user-select: none;
    }

    .image-popup {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.9);
      z-index: 1000;
      justify-content: center;
      align-items: center;
    }

    .image-popup img {
      max-width: 90%;
      max-height: 90%;
      object-fit: contain;
    }

    .close-popup {
      position: absolute;
      top: 20px;
      right: 20px;
      background-color: rgba(255, 255, 255, 0.7);
      border: none;
      border-radius: 50%;
      cursor: pointer;
      font-size: 20px;
      width: 5vh;
      height: 5vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }
  </style>
</head>

<body class="bg-gray-100 font-sans">
  <div id="app" class="min-h-screen flex flex-col">
    <header class="bg-blue-600 text-white p-4 text-xl font-bold">
      <h1 class="text-center">Eventos dos próximos 7 días</h1>
    </header>

    <main class="flex-grow p-4">
      <div id="eventList" class="grid grid-cols-1 gap-4">
      </div>
    </main>

    <footer class="bg-gray-800 text-white p-4 text-center text-xl flex justify-between">
      <p class="text-left">IES de Teis</p>
      <p class="text-right">Alejandro Verde</p>
    </footer>
  </div>

  <div id="imagePopup" class="image-popup">
    <button class="close-popup" onclick="closeImagePopup(); event.stopPropagation();">X</button>
    <img id="popupImage" src="" alt="Full size event image">
  </div>

  <script src="/assets/js/events.js"></script>
  <script>
    const imagePopup = document.getElementById("imagePopup");

    function closeImagePopup() {
      imagePopup.style.display = "none";
    }
  </script>
</body>

</html>